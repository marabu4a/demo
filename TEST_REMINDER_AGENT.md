# Тестирование AI-агента напоминаний

## Быстрый тест уведомлений (1 минута)

Для тестирования системных уведомлений macOS можно запустить агента с очень короткими интервалами.

### Способ 1: Через Gradle задачу (рекомендуется)

```bash
./gradlew :server:testAiReminderAgent
```

Это запустит агента с параметрами:
- Проверка каждую **1 минуту**
- Сводка каждые **2 минуты**

### Способ 2: Через параметры командной строки

```bash
./gradlew :server:runAiReminderAgent -PagentArgs="1 0.033"
```

Где:
- `1` - проверка каждую 1 минуту
- `0.033` - сводка каждые 2 минуты (0.033 часа ≈ 2 минуты)

### Способ 3: Прямой запуск

```bash
./gradlew :server:build
java -cp server/build/libs/server-1.0.0.jar org.example.demo.AiReminderAgentKt 1 0.033
```

## Что вы увидите

### В консоли:

```
============================================================
🤖 AI-АГЕНТ НАПОМИНАНИЙ 24/7
============================================================

🧪 ТЕСТОВЫЙ РЕЖИМ АКТИВИРОВАН
   Проверка каждые: 1 минут(ы)
   Сводка каждые: 0.033 часов
   Уведомления будут приходить очень часто!

🚀 Запуск AI-агента напоминаний...
   URL MCP сервера: http://localhost:8080/mcp
   URL AI API: https://gigachat.devices.sberbank.ru/api/v1/chat/completions
   Интервал проверки: 1 минут
   Интервал сводки: 0.033 часов

✅ AI-агент запущен и работает в фоновом режиме
   Нажмите Ctrl+C для остановки

🧪 ТЕСТОВЫЙ РЕЖИМ: Первая проверка через 1 минут(ы)

🔔 ПРОСРОЧЕННЫЕ НАПОМИНАНИЯ (AI анализ):
...
```

### Системные уведомления macOS:

Через 1 минуту вы получите уведомление в правом верхнем углу экрана:
- **Заголовок:** "Просроченные напоминания"
- **Подзаголовок:** "Требуют внимания"
- **Сообщение:** Краткая информация о задачах

Через 2 минуты вы получите уведомление о полной сводке:
- **Заголовок:** "Сводка по напоминаниям"
- **Подзаголовок:** "AI анализ"
- **Сообщение:** Краткая сводка

## Другие варианты тестирования

### Тест каждые 30 секунд

```bash
./gradlew :server:runAiReminderAgent -PagentArgs="0.5 0.016"
```

Где:
- `0.5` - проверка каждые 30 секунд (0.5 минуты)
- `0.016` - сводка каждую минуту (0.016 часа ≈ 1 минута)

### Тест каждые 2 минуты

```bash
./gradlew :server:runAiReminderAgent -PagentArgs="2 0.05"
```

Где:
- `2` - проверка каждые 2 минуты
- `0.05` - сводка каждые 3 минуты (0.05 часа ≈ 3 минуты)

## Важно для тестирования

1. **MCP сервер должен быть запущен** на `http://localhost:8080/mcp`
2. **Создайте хотя бы одно напоминание** для тестирования:
   ```bash
   curl -X POST http://localhost:8080/mcp \
     -H "Content-Type: application/json" \
     -d '{
       "jsonrpc": "2.0",
       "id": 1,
       "method": "tools/call",
       "params": {
         "name": "reminder",
         "arguments": {
           "action": "create",
           "title": "Тестовое напоминание",
           "description": "Для проверки уведомлений",
           "priority": "high"
         }
       }
     }'
   ```

3. **Проверьте настройки уведомлений macOS:**
   - Системные настройки → Уведомления
   - Убедитесь, что уведомления от Terminal включены

## Остановка теста

Нажмите `Ctrl+C` в терминале, где запущен агент.

## Возврат к нормальному режиму

Для работы в нормальном режиме (60 минут / 6 часов):

```bash
./gradlew :server:runAiReminderAgent
```

Или без параметров - будут использованы значения по умолчанию.

