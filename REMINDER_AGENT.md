# Инструмент Reminder и Агент 24/7

## Описание

Инструмент `reminder` позволяет управлять напоминаниями через MCP протокол. Напоминания сохраняются в JSON файле и могут быть использованы для создания агента, который работает 24/7 и периодически выдает сводку.

## Компоненты

1. **ReminderService** - сервис для управления напоминаниями (хранение в JSON)
2. **MCP инструмент `reminder`** - интеграция в MCP сервер
3. **ReminderAgent** - агент, работающий 24/7 и выдающий сводку
4. **AiReminderAgent** - AI-агент, работающий 24/7 и использующий AI для анализа и форматирования сводки

## Быстрый старт

### 1. Запустите MCP сервер

```bash
./gradlew :server:run
```

Сервер будет доступен на `http://localhost:8080/mcp`

### 2. Используйте инструмент reminder

#### Создание напоминания

```bash
curl -X POST http://localhost:8080/mcp \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "id": 1,
    "method": "tools/call",
    "params": {
      "name": "reminder",
      "arguments": {
        "action": "create",
        "title": "Встреча с командой",
        "description": "Обсуждение нового проекта",
        "dueDate": 1735689600000,
        "priority": "high",
        "category": "работа"
      }
    }
  }'
```

#### Получение списка напоминаний

```bash
curl -X POST http://localhost:8080/mcp \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "id": 2,
    "method": "tools/call",
    "params": {
      "name": "reminder",
      "arguments": {
        "action": "list",
        "includeCompleted": false
      }
    }
  }'
```

#### Получение сводки

```bash
curl -X POST http://localhost:8080/mcp \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "id": 3,
    "method": "tools/call",
    "params": {
      "name": "reminder",
      "arguments": {
        "action": "get_summary"
      }
    }
  }'
```

### 3. Запустите агента 24/7

Агент работает в фоновом режиме и периодически проверяет напоминания и выдает сводку.

#### Запуск через Gradle

```bash
./gradlew :server:run --args="reminder-agent"
```

Или создайте отдельную задачу в `server/build.gradle.kts`:

```kotlin
tasks.register<JavaExec>("runReminderAgent") {
    group = "application"
    classpath = sourceSets["main"].runtimeClasspath
    mainClass.set("org.example.demo.ReminderAgentKt")
    args = listOf("60", "6", "http://localhost:8080/mcp")
}
```

Затем запустите:

```bash
./gradlew :server:runReminderAgent
```

#### Параметры запуска

```bash
java -cp <classpath> org.example.demo.ReminderAgentKt [checkIntervalMinutes] [summaryIntervalHours] [serverUrl]
```

- `checkIntervalMinutes` - интервал проверки напоминаний в минутах (по умолчанию: 60)
- `summaryIntervalHours` - интервал полной сводки в часах (по умолчанию: 6)
- `serverUrl` - URL MCP сервера (по умолчанию: http://localhost:8080/mcp)

#### Примеры

```bash
# Проверка каждые 30 минут, сводка каждые 3 часа
java -cp ... org.example.demo.ReminderAgentKt 30 3

# Проверка каждые 15 минут, сводка каждый час
java -cp ... org.example.demo.ReminderAgentKt 15 1
```

## Доступные действия

### `create` - Создать напоминание

**Параметры:**
- `title` (обязательно) - название напоминания
- `description` (опционально) - описание
- `dueDate` (опционально) - время напоминания в миллисекундах (timestamp)
- `priority` (опционально) - приоритет: `low`, `normal`, `high` (по умолчанию: `normal`)
- `category` (опционально) - категория

**Пример:**
```json
{
  "action": "create",
  "title": "Купить молоко",
  "description": "Не забыть купить молоко в магазине",
  "dueDate": 1735689600000,
  "priority": "normal",
  "category": "покупки"
}
```

### `list` - Список напоминаний

**Параметры:**
- `includeCompleted` (опционально) - включать выполненные (по умолчанию: `true`)

**Пример:**
```json
{
  "action": "list",
  "includeCompleted": false
}
```

### `get` - Получить напоминание по ID

**Параметры:**
- `id` (обязательно) - ID напоминания

**Пример:**
```json
{
  "action": "get",
  "id": "reminder_1234567890_0"
}
```

### `delete` - Удалить напоминание

**Параметры:**
- `id` (обязательно) - ID напоминания

**Пример:**
```json
{
  "action": "delete",
  "id": "reminder_1234567890_0"
}
```

### `complete` - Отметить как выполненное

**Параметры:**
- `id` (обязательно) - ID напоминания

**Пример:**
```json
{
  "action": "complete",
  "id": "reminder_1234567890_0"
}
```

### `get_summary` - Получить сводку

Полная сводка по всем напоминаниям с статистикой и списками просроченных, на сегодня и с высоким приоритетом.

**Параметры:** нет

**Пример:**
```json
{
  "action": "get_summary"
}
```

### `get_due` - Получить просроченные напоминания

**Параметры:** нет

**Пример:**
```json
{
  "action": "get_due"
}
```

## Хранение данных

Напоминания сохраняются в файл `reminders.json` в корне проекта (там, где запущен сервер).

**Формат файла:**
```json
[
  {
    "id": "reminder_1234567890_0",
    "title": "Встреча с командой",
    "description": "Обсуждение нового проекта",
    "createdAt": 1735603200000,
    "dueDate": 1735689600000,
    "priority": "high",
    "category": "работа",
    "completed": false,
    "completedAt": null
  }
]
```

## Работа агента 24/7

Агент работает в фоновом режиме и:

1. **Периодически проверяет** просроченные напоминания (каждый час по умолчанию)
2. **Выдает полную сводку** через заданный интервал (каждые 6 часов по умолчанию)
3. **Выводит информацию** в консоль

### Пример вывода агента

```
============================================================
🤖 АГЕНТ НАПОМИНАНИЙ 24/7
============================================================

🚀 Запуск агента напоминаний...
   URL сервера: http://localhost:8080/mcp
   Интервал проверки: 60 минут
   Интервал сводки: 6 часов

✅ Агент запущен и работает в фоновом режиме
   Нажмите Ctrl+C для остановки

🔔 ПРОСРОЧЕННЫЕ НАПОМИНАНИЯ:
Просроченные напоминания (2):

ID: reminder_1234567890_0
Название: Встреча с командой
Описание: Обсуждение нового проекта
Напомнить: 2024-12-31 10:00
Приоритет: high

============================================================
📋 СВОДКА ПО НАПОМИНАНИЯМ
   Время: 2024-12-31T12:00:00Z
============================================================

📋 СВОДКА ПО НАПОМИНАНИЯМ
========================================
Всего напоминаний: 5
✅ Выполнено: 2
⏳ Ожидает: 3
🔴 Просрочено: 1
📅 На сегодня: 2
⚡ Высокий приоритет: 1

⚠️ ПРОСРОЧЕННЫЕ НАПОМИНАНИЯ:
  • Встреча с командой (было: 2024-12-31 10:00)
    Обсуждение нового проекта

📅 НА СЕГОДНЯ:
  • Купить молоко (до: 2024-12-31 18:00)
    Не забыть купить молоко в магазине

⚡ ВЫСОКИЙ ПРИОРИТЕТ:
  • Встреча с командой
    Обсуждение нового проекта
============================================================
```

## Интеграция с AI агентом

Инструмент `reminder` можно использовать через AI агента:

1. Подключите MCP сервер в приложении
2. Задайте вопрос агенту: "Создай напоминание о встрече завтра в 10:00"
3. Агент автоматически вызовет инструмент `reminder` с действием `create`
4. Для получения сводки: "Покажи сводку по напоминаниям"

## Примеры использования

### Создание напоминания с временем

```kotlin
// Получить timestamp для завтра в 10:00
val tomorrow = java.time.LocalDateTime.now()
    .plusDays(1)
    .withHour(10)
    .withMinute(0)
    .withSecond(0)
val timestamp = tomorrow.atZone(java.time.ZoneId.systemDefault())
    .toInstant()
    .toEpochMilli()

// Создать напоминание
val result = mcpClient.callTool(
    name = "reminder",
    arguments = buildJsonObject {
        put("action", "create")
        put("title", "Встреча с командой")
        put("description", "Обсуждение нового проекта")
        put("dueDate", timestamp)
        put("priority", "high")
        put("category", "работа")
    }
)
```

### Получение сводки

```kotlin
val result = mcpClient.callTool(
    name = "reminder",
    arguments = buildJsonObject {
        put("action", "get_summary")
    }
)

val summary = result?.content?.firstOrNull()?.text
println(summary)
```

## Расширение функциональности

### Добавление новых полей в Reminder

1. Обновите модель `Reminder` в `ReminderService.kt`
2. Обновите методы создания и обновления
3. Обновите схему инструмента в `McpServer.kt`

### Изменение интервала проверки агента

Измените параметры при запуске:

```bash
./gradlew :server:runReminderAgent --args="30 3"
```

### Использование SQLite вместо JSON

Замените `ReminderService` на версию с SQLite:

1. Добавьте зависимость SQLite
2. Создайте таблицу для напоминаний
3. Обновите методы загрузки/сохранения

## Результат

✅ У вас есть агент, работающий 24/7 и выдающий сводку по напоминаниям!

**Обычный агент:**
- ✅ Сохраняет задачи в JSON
- ✅ Периодически проверяет напоминания
- ✅ Выдает сводку через заданный интервал
- ✅ Работает в фоновом режиме
- ✅ Может быть остановлен через Ctrl+C

**AI-агент (рекомендуется):**
- ✅ Все функции обычного агента
- ✅ Использует AI для анализа сводки
- ✅ Красивое форматирование и структурирование
- ✅ Рекомендации и приоритизация
- ✅ Дружелюбные и мотивирующие сообщения
- ✅ Автоматическое выделение важных моментов

## Пример вывода AI-агента

Вместо сырых данных:

```
📋 СВОДКА ПО НАПОМИНАНИЯМ
========================================
Всего напоминаний: 5
✅ Выполнено: 2
⏳ Ожидает: 3
...
```

AI-агент выдаст:

```
📋 СВОДКА ПО НАПОМИНАНИЯМ (AI анализ)
============================================================

Привет! Вот твоя сводка по напоминаниям:

📊 Статистика:
- Всего напоминаний: 5
- Выполнено: 2 (отлично!)
- Ожидает выполнения: 3

⚠️ Важные моменты:
1. У тебя есть 1 просроченное напоминание - "Встреча с командой"
   Это требует немедленного внимания!

2. На сегодня запланировано 2 задачи с высоким приоритетом

💡 Рекомендации:
- Начни с просроченной задачи "Встреча с командой"
- Затем переходи к задачам с высоким приоритетом
- Не забывай отмечать выполненные задачи

Ты отлично справляешься! Продолжай в том же духе! 💪
```

