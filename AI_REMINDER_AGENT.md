# AI-Агент для напоминаний 24/7

## Описание

AI-агент (`AiReminderAgent`) - это улучшенная версия обычного агента напоминаний, которая использует AI (GigaChat) для анализа и форматирования сводки по напоминаниям. Вместо сырых данных вы получаете структурированную, дружелюбную и информативную сводку.

## Преимущества AI-агента

- ✅ **Красивое форматирование** - сводка структурирована и легко читается
- ✅ **Анализ важности** - AI выделяет срочные и важные задачи
- ✅ **Рекомендации** - получаете советы по приоритетам
- ✅ **Мотивация** - дружелюбные и мотивирующие сообщения
- ✅ **Контекст** - AI понимает контекст ваших задач

## Быстрый старт

### 1. Запустите MCP сервер

```bash
./gradlew :server:run
```

### 3. Запустите AI-агента

```bash
./gradlew :server:runAiReminderAgent
```

## Как это работает

### Процесс работы AI-агента

1. **Получение данных** - агент вызывает MCP инструмент `reminder` для получения сводки
2. **Отправка в AI** - сырые данные отправляются в GigaChat API
3. **Анализ AI** - AI анализирует данные и создает структурированную сводку
4. **Вывод результата** - красиво отформатированная сводка выводится в консоль

### Пример работы

**Входные данные (сырые):**
```
📋 СВОДКА ПО НАПОМИНАНИЯМ
========================================
Всего напоминаний: 5
✅ Выполнено: 2
⏳ Ожидает: 3
🔴 Просрочено: 1
📅 На сегодня: 2
⚡ Высокий приоритет: 1

⚠️ ПРОСРОЧЕННЫЕ НАПОМИНАНИЯ:
  • Встреча с командой (было: 2024-12-31 10:00)
    Обсуждение нового проекта
```

**Выходные данные (после AI анализа):**
```
📋 СВОДКА ПО НАПОМИНАНИЯМ (AI анализ)
============================================================

Привет! Вот твоя сводка по напоминаниям:

📊 Статистика:
- Всего напоминаний: 5
- Выполнено: 2 (отлично!)
- Ожидает выполнения: 3

⚠️ Важные моменты:
1. У тебя есть 1 просроченное напоминание - "Встреча с командой"
   Это требует немедленного внимания!

2. На сегодня запланировано 2 задачи с высоким приоритетом

💡 Рекомендации:
- Начни с просроченной задачи "Встреча с командой"
- Затем переходи к задачам с высоким приоритетом
- Не забывай отмечать выполненные задачи

Ты отлично справляешься! Продолжай в том же духе! 💪
```

## Настройка

### Параметры запуска

```bash
./gradlew :server:runAiReminderAgent -PagentArgs="30 3 http://localhost:8080/mcp"
```

Параметры:
1. `checkIntervalMinutes` - интервал проверки в минутах (по умолчанию: 60)
2. `summaryIntervalHours` - интервал сводки в часах (по умолчанию: 6)
3. `serverUrl` - URL MCP сервера (по умолчанию: http://localhost:8080/mcp)

**Примечание:** Authorization Key захардкожен в классе и не передается как параметр.

### Примеры

```bash
# Проверка каждые 30 минут, сводка каждые 3 часа
./gradlew :server:runAiReminderAgent -PagentArgs="30 3"

# Проверка каждые 15 минут, сводка каждый час
./gradlew :server:runAiReminderAgent -PagentArgs="15 1"
```

## Работа без AI

Если API ключ не установлен, агент будет работать в режиме обычного агента:
- Выводит сырые данные без AI анализа
- Показывает предупреждение о недоступности AI
- Продолжает работать с базовой функциональностью

## Архитектура

```
┌─────────────────┐
│  AiReminderAgent│
└────────┬────────┘
         │
         ├─► MCP Server (reminder tool)
         │   └─► Получение данных
         │
         └─► GigaChat API
             └─► AI анализ и форматирование
```

## Сравнение с обычным агентом

| Функция | Обычный агент | AI-агент |
|---------|---------------|----------|
| Проверка напоминаний | ✅ | ✅ |
| Выдача сводки | ✅ | ✅ |
| Форматирование | Базовое | Красивое (AI) |
| Анализ важности | ❌ | ✅ |
| Рекомендации | ❌ | ✅ |
| Мотивация | ❌ | ✅ |
| Требует API ключ | ❌ | ✅ |

## Устранение проблем

### AI недоступен

Если AI не работает, проверьте:
1. Правильность Authorization Key в коде класса `AiReminderAgent` (константа `AI_API_KEY`)
2. Доступность API GigaChat
3. Сетевое подключение

Authorization Key захардкожен в классе, поэтому не требуется дополнительная настройка.

### Ошибка получения токена

Если видите:
```
⚠️ Ошибка при получении токена GigaChat
```

Проверьте:
- Правильность API ключа
- Доступность API GigaChat
- Сетевое подключение

### Ошибка AI API

Если видите:
```
❌ Ошибка AI API: ...
```

Возможные причины:
- Неверный токен доступа
- Превышен лимит запросов
- Проблемы с сетью

Агент продолжит работу без AI анализа.

## Системные уведомления macOS

Агент автоматически отправляет системные уведомления macOS при:
- **Просроченных напоминаниях** - каждый час, если есть просроченные задачи
- **Полной сводке** - каждые 6 часов (или по настройке)

### Как это работает

Уведомления отправляются через системную команду `osascript` и появляются в правом верхнем углу экрана macOS (или в Центре уведомлений).

**Пример уведомления:**
- **Заголовок:** "Просроченные напоминания"
- **Подзаголовок:** "Требуют внимания"
- **Сообщение:** Краткая информация о просроченных задачах

### Настройка уведомлений macOS

Если уведомления не появляются, проверьте настройки macOS:
1. Системные настройки → Уведомления
2. Найдите приложение Terminal (или то, где запущен агент)
3. Убедитесь, что уведомления включены

**Примечание:** При первом запуске macOS может запросить разрешение на отправку уведомлений от Terminal.

### Отключение уведомлений

Если нужно отключить системные уведомления, можно закомментировать вызовы `sendMacOSNotification()` в коде `AiReminderAgent.kt`.

## Результат

✅ У вас есть AI-агент, который:
- Работает 24/7 в фоновом режиме
- Использует AI для анализа напоминаний
- Выдает красивые и информативные сводки
- Дает рекомендации и мотивирует
- Автоматически выделяет важные моменты
- **Отправляет системные уведомления macOS** 🎯

