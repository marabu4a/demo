# Тестирование Яндекс Трекера через AI агента

## Описание

Теперь AI агент автоматически определяет, когда пользователь спрашивает о задачах в трекере, и вызывает MCP инструмент `yandex_tracker` для получения актуальной информации.

## Как это работает

1. **Пользователь задает вопрос** о задачах/трекере
2. **Агент анализирует запрос** и определяет, что нужен вызов MCP инструмента
3. **Агент автоматически вызывает** инструмент `yandex_tracker` через MCP
4. **Результат добавляется в контекст** для AI
5. **AI формирует ответ** на основе полученных данных

## Настройка

### Шаг 1: Запустите MCP сервер

В терминале:
```bash
./gradlew :server:run
```

Сервер будет доступен на `http://localhost:8080/mcp`

### Шаг 2: Подключите MCP сервер в приложении

1. Запустите приложение
2. Нажмите кнопку **"MCP"** в верхней панели
3. Нажмите **"+ Добавить сервер"**
4. Введите:
   - **Имя**: `Local Server` (или любое другое)
   - **URL**: `http://localhost:8080/mcp`
5. Нажмите **"Подключить"**

### Шаг 3: Задайте вопрос агенту

Теперь вы можете задать вопросы о задачах, и агент автоматически вызовет инструмент!

## Примеры вопросов

### Подсчет задач

**Вопрос:**
```
Сколько открытых задач в Яндекс Трекере?
```

**Что происходит:**
1. Агент определяет ключевые слова: "сколько", "задач", "трекер"
2. Вызывает инструмент `yandex_tracker` с действием `count_open_tasks`
3. Получает результат: "Количество открытых задач в очереди 'TEST': 5"
4. Добавляет результат в контекст для AI
5. AI отвечает: "Согласно данным Яндекс Трекера, в очереди TEST открыто 5 задач."

### Список задач

**Вопрос:**
```
Покажи список открытых задач
```

**Что происходит:**
1. Агент определяет ключевые слова: "список", "задач"
2. Вызывает инструмент с действием `get_open_tasks`
3. Получает список всех открытых задач
4. AI формирует ответ с детальной информацией

### Информация о конкретной задаче

**Вопрос:**
```
Расскажи о задаче TEST-1
```

**Что происходит:**
1. Агент определяет упоминание конкретной задачи
2. Вызывает инструмент с действием `get_task` и `task_id: "TEST-1"`
3. Получает детальную информацию о задаче
4. AI предоставляет структурированный ответ

## Ключевые слова для автоматического вызова

Агент автоматически вызывает инструмент при упоминании:

- **Трекер**: "трекер", "yandex tracker"
- **Задачи**: "задач", "таск", "issue"
- **Вопросы**: "сколько задач", "открытые задачи", "список задач"

## Логика определения действия

Агент автоматически определяет, какое действие вызвать:

- **"сколько"** или **"количество"** → `count_open_tasks`
- **"список"** или **"все"** → `get_open_tasks`
- **Упоминание конкретной задачи** (например, "TEST-1") → `get_task`
- **По умолчанию** → `count_open_tasks`

## Просмотр логов

Для отладки можно посмотреть логи:

1. В IntelliJ IDEA: View → Tool Windows → Logcat (для Android) или Run → View Logs
2. Ищите сообщения с тегом `ChatViewModel`:
   - `"Detected tracker-related query"`
   - `"Found yandex_tracker tool on server"`
   - `"Successfully called yandex_tracker tool"`

## Примеры диалогов

### Пример 1: Простой подсчет

**Пользователь:**
```
Сколько открытых задач?
```

**Агент:**
```
Согласно данным Яндекс Трекера, в очереди TEST открыто 5 задач.
```

### Пример 2: Детальный список

**Пользователь:**
```
Покажи все открытые задачи в трекере
```

**Агент:**
```
Вот список открытых задач в очереди TEST:

1. TEST-1: Исправить баг в авторизации
   Описание: Пользователи не могут войти в систему

2. TEST-2: Добавить новую функцию поиска
   Описание: Реализовать полнотекстовый поиск

3. TEST-4: Оптимизировать запросы к БД
   Описание: Улучшить производительность

4. TEST-5: Добавить тесты для модуля платежей
   Описание: Покрыть тестами критический функционал

5. TEST-7: Настроить CI/CD
   Описание: Автоматизировать деплой

Всего: 5 открытых задач.
```

### Пример 3: Информация о задаче

**Пользователь:**
```
Что известно о задаче TEST-1?
```

**Агент:**
```
Задача TEST-1:
- Статус: open
- Название: Исправить баг в авторизации
- Описание: Пользователи не могут войти в систему

Задача находится в очереди TEST и требует внимания.
```

## Отладка

### Проблема: Агент не вызывает инструмент

**Проверьте:**
1. ✅ MCP сервер запущен (`./gradlew :server:run`)
2. ✅ Сервер подключен в приложении (кнопка "MCP" → проверьте статус подключения)
3. ✅ Вопрос содержит ключевые слова (трекер, задач, таск)
4. ✅ Проверьте логи на наличие ошибок

### Проблема: Ошибка при вызове инструмента

**Проверьте:**
1. ✅ MCP сервер отвечает на запросы (проверьте через `test-mcp-requests.http`)
2. ✅ Инструмент `yandex_tracker` доступен (вызовите `tools/list`)
3. ✅ Проверьте логи для детальной информации об ошибке

## Расширение функциональности

### Добавление новых ключевых слов

Отредактируйте `checkAndCallMcpTools()` в `ChatViewModel.kt`:

```kotlin
val trackerKeywords = listOf(
    "трекер", "задач", "таск", "issue", "yandex tracker",
    "сколько задач", "открытые задачи", "список задач",
    "ваши ключевые слова" // Добавьте свои
)
```

### Добавление новых действий

Добавьте новые условия в `when` блок:

```kotlin
val action = when {
    // Существующие условия...
    lowerMessage.contains("создать") -> "create_task"
    lowerMessage.contains("обновить") -> "update_task"
    // ...
}
```

## Результат

✅ **AI агент автоматически вызывает MCP инструменты**  
✅ **Получает актуальные данные из трекера**  
✅ **Формирует информативные ответы на основе реальных данных**  

🎉 **Интеграция завершена!**


