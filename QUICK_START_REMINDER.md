# Быстрый старт: Reminder и Агент 24/7

## За 3 шага

### 1. Запустите MCP сервер

```bash
./gradlew :server:run
```

Сервер будет доступен на `http://localhost:8080/mcp`

### 2. Создайте напоминание

**Через curl:**
```bash
curl -X POST http://localhost:8080/mcp \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "id": 1,
    "method": "tools/call",
    "params": {
      "name": "reminder",
      "arguments": {
        "action": "create",
        "title": "Тестовое напоминание",
        "description": "Проверка работы инструмента",
        "priority": "high"
      }
    }
  }'
```

**Через AI агента:**
- Подключите MCP сервер в приложении
- Спросите: "Создай напоминание о встрече завтра в 10:00"

### 3. Запустите агента 24/7

В **новом терминале** (MCP сервер должен продолжать работать):

#### Вариант A: Обычный агент

```bash
./gradlew :server:runReminderAgent
```

#### Вариант B: AI-агент (рекомендуется) ⭐

AI-агент использует ваш AI для анализа и форматирования сводки.

**Запуск:**
```bash
./gradlew :server:runAiReminderAgent
```

**Примечание:** Authorization Key для GigaChat захардкожен в классе и не требует дополнительной настройки.

**Примечание:** `GIGACHAT_API_KEY` - это на самом деле `AUTHORIZATION_KEY` (Base64-encoded строка), которая используется для получения access token через OAuth endpoint GigaChat.

**Агент будет:**
- ✅ Проверять напоминания каждые 60 минут
- ✅ Выдавать полную сводку каждые 6 часов
- ✅ Использовать AI для анализа и форматирования
- ✅ Работать в фоновом режиме
- ✅ Отправлять системные уведомления macOS 🎯

## Настройка интервалов

```bash
# Проверка каждые 30 минут, сводка каждые 3 часа
./gradlew :server:runReminderAgent -PagentArgs="30 3"

# Проверка каждые 15 минут, сводка каждый час
./gradlew :server:runReminderAgent -PagentArgs="15 1"
```

## Получение сводки вручную

```bash
curl -X POST http://localhost:8080/mcp \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "id": 1,
    "method": "tools/call",
    "params": {
      "name": "reminder",
      "arguments": {
        "action": "get_summary"
      }
    }
  }'
```

## Результат

✅ У вас есть агент, работающий 24/7 и выдающий сводку по напоминаниям!

## Подробная документация

См. `REMINDER_AGENT.md` для полной информации.

