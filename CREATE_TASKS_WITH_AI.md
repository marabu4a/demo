# Создание задач в Яндекс Трекере через AI агента

## Описание

Теперь AI агент может автоматически создавать новые задачи в Яндекс Трекере через MCP инструмент. Агент анализирует запрос пользователя, извлекает название и описание задачи, и создает её в трекере.

## Как это работает

1. **Пользователь просит создать задачу** (например: "Создай задачу: Исправить баг")
2. **Агент определяет намерение** создать задачу
3. **Агент извлекает информацию** (название, описание) из запроса
4. **Агент вызывает MCP инструмент** `yandex_tracker` с действием `create_task`
5. **MCP сервер создает задачу** и возвращает результат
6. **Агент сообщает пользователю** о созданной задаче

## Примеры использования

### Простое создание задачи

**Пользователь:**
```
Создай задачу: Исправить баг в авторизации
```

**Агент:**
```
Задача успешно создана в Яндекс Трекере!

Ключ: TEST-8
Название: Исправить баг в авторизации
Статус: open
```

### Создание задачи с описанием

**Пользователь:**
```
Создай задачу: Добавить новую функцию; Реализовать полнотекстовый поиск по базе данных
```

**Агент:**
```
Задача успешно создана в Яндекс Трекере!

Ключ: TEST-9
Название: Добавить новую функцию
Описание: Реализовать полнотекстовый поиск по базе данных
Статус: open
```

### Различные формулировки

Агент понимает различные формулировки:

- ✅ "Создай задачу: название"
- ✅ "Создать задачу название"
- ✅ "Добавить задачу: название"
- ✅ "Новая задача: название"
- ✅ "Создай задачу название, описание"

## Поддерживаемые форматы

### Формат 1: Простое название
```
Создай задачу: Название задачи
```

### Формат 2: Название и описание через запятую
```
Создай задачу: Название задачи, Описание задачи
```

### Формат 3: Название и описание через точку с запятой
```
Создай задачу: Название задачи; Описание задачи
```

### Формат 4: Название и описание на разных строках
```
Создай задачу: Название задачи
Описание задачи
```

## Настройка

### Шаг 1: Запустите MCP сервер

```bash
./gradlew :server:run
```

### Шаг 2: Подключите сервер в приложении

1. Запустите приложение
2. Нажмите **"MCP"** → **"+ Добавить сервер"**
3. Введите:
   - Имя: `Local Server`
   - URL: `http://localhost:8080/mcp`
4. Нажмите **"Подключить"**

### Шаг 3: Создайте задачу через агента

Просто попросите агента создать задачу!

## Технические детали

### MCP Сервер

Инструмент `yandex_tracker` поддерживает новое действие:

```json
{
  "action": "create_task",
  "summary": "Название задачи",
  "description": "Описание задачи (опционально)",
  "queue": "TEST"
}
```

### Извлечение информации из запроса

Агент использует регулярные выражения для извлечения названия и описания:

1. **Паттерны поиска:**
   - `создать задачу: название`
   - `добавить задачу: название`
   - `новая задача: название`
   - `создай задачу: название`

2. **Разделители:**
   - Запятая (`,`)
   - Точка с запятой (`;`)
   - Перенос строки (`\n`)

3. **Fallback:**
   - Если паттерн не найден, используется текст после ключевых слов
   - Если название пустое, используется "Новая задача"

## Примеры диалогов

### Пример 1: Простое создание

**Пользователь:**
```
Создай задачу: Оптимизировать запросы к БД
```

**Агент:**
```
Я создал задачу в Яндекс Трекере:

Ключ: TEST-10
Название: Оптимизировать запросы к БД
Статус: open

Задача успешно добавлена в очередь TEST.
```

### Пример 2: С описанием

**Пользователь:**
```
Создай задачу: Добавить тесты для модуля платежей; Покрыть тестами критический функционал обработки платежей
```

**Агент:**
```
Я создал задачу в Яндекс Трекере:

Ключ: TEST-11
Название: Добавить тесты для модуля платежей
Описание: Покрыть тестами критический функционал обработки платежей
Статус: open

Задача готова к работе!
```

### Пример 3: Естественный язык

**Пользователь:**
```
Мне нужно создать задачу на исправление бага в авторизации. Пользователи не могут войти в систему.
```

**Агент:**
```
Я создал задачу в Яндекс Трекере:

Ключ: TEST-12
Название: Исправление бага в авторизации
Описание: Пользователи не могут войти в систему
Статус: open

Задача создана и готова к работе!
```

## Проверка созданных задач

После создания задачи вы можете:

1. **Проверить список задач:**
   ```
   Покажи список открытых задач
   ```

2. **Посмотреть конкретную задачу:**
   ```
   Расскажи о задаче TEST-10
   ```

3. **Подсчитать задачи:**
   ```
   Сколько открытых задач?
   ```

## Ограничения

### Текущая реализация (мок данные)

- Задачи сохраняются в памяти сервера
- При перезапуске сервера задачи теряются
- Ключи задач генерируются последовательно (TEST-1, TEST-2, ...)

### Для продакшена

Для реального использования нужно:

1. **Подключить реальный API Яндекс Трекера:**
   - Заменить `mockTasks` на реальные HTTP запросы
   - Использовать OAuth токены для авторизации
   - Сохранять задачи в реальном трекере

2. **Добавить валидацию:**
   - Проверка формата названия
   - Ограничение длины описания
   - Валидация очереди

3. **Добавить обработку ошибок:**
   - Ошибки создания задачи
   - Недоступность API
   - Превышение лимитов

## Расширение функциональности

### Добавление новых полей

Можно расширить создание задач:

```kotlin
// В MCP сервере
data class YandexTrackerTask(
    val key: String,
    val summary: String,
    val status: String,
    val description: String? = null,
    val assignee: String? = null,
    val priority: String? = null,  // Новое поле
    val tags: List<String>? = null  // Новое поле
)
```

### Добавление обновления задач

Можно добавить действие `update_task`:

```kotlin
"update_task" -> {
    val taskId = arguments?.get("task_id")?.jsonPrimitive?.content
    val newStatus = arguments?.get("status")?.jsonPrimitive?.content
    // Обновление задачи
}
```

## Результат

✅ **Агент автоматически создает задачи** в Яндекс Трекере  
✅ **Извлекает название и описание** из естественного языка  
✅ **Поддерживает различные формулировки** запросов  
✅ **Интегрирован с MCP протоколом**  

🎉 **Функциональность готова к использованию!**

